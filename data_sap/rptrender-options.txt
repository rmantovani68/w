rptrender [options]

-help        display usage information
--help       display usage information

-databaseURL=drv://host:port/dbname
                log in to database dbname on the given host and port
                using the Qt database driver drv
-d dbname       log in to database dbname
-h addr         look for the database server on host addr
-p #            look for the database server on port #
-P drv          use the Qt database driver drv
-U x            log in as user x
-username=x     log in as user x
-passwd=w       log in with password w
-noauth         (deprecated & ignored)

-numCopies=#    produce # copies of the report
-printpreview   preview the report with an option to print
-print          print the report, prompting for the printer to use
-autoprint      print to the default printer
-printerName=P  send output to the system printer P
-pdf            generate PDF output
-outpdf=FILE    send PDF output to FILE

-loadfromdb=RPT load the named RPT from the database

-param='[+-]name[:type][=value]'
         define/set a MetaSQL parameter with optional value and type
         '-' means the parameter is defined but inactive
         '+' means the parameter is active (default)
-e       use the value 'Missing' for undefined parameters

-close   quit when finished generating the report
-fromStdin=codec
         read and parse arguments from the standard input instead of
         the command line, using the named codec, one argument per line.
         Stop reading when the line starts with -launch

rptrender -databaseURL=pgsql://database/sap -username=roberto -passwd=3zin -reportname=/u/prj/wella/data_sap/packing_list_ordine.xml -param=ordkey=3024131295.1.01 -pdf

